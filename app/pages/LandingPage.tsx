import type { PageProperties } from "~/route-containers/entry.component";

const LandingPage = ({ data }: PageProperties) => {
  return (
    <div>
      <h1>Landing-Page</h1>
    </div>
  );
};

export default LandingPage;
